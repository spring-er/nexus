# =============================================================================
# Nexus AI — Dependencies
# =============================================================================
# Install with: pip install -r requirements.txt
# =============================================================================

# --- UI ---
gradio>=4.0.0

# --- LLM Communication ---
# OpenAI SDK works with OpenRouter, Groq, and Ollama (all OpenAI-compatible)
openai>=1.0.0

# Google Gemini SDK (free tier)
google-generativeai>=0.5.0

# --- Phase 2: RAG & Knowledge Base ---
# LlamaIndex: Framework for RAG pipelines (chunking, indexing, querying)
llama-index-core>=0.11.0
llama-index-readers-file>=0.2.0       # PDF, TXT, MD file readers
beautifulsoup4>=4.12.0                # HTML parsing for web URL ingestion
llama-index-embeddings-huggingface>=0.3.0  # Free local embeddings

# LanceDB: Local vector database (no server, stores on disk)
lancedb>=0.17.0

# YouTube transcript ingestion (no API key needed)
youtube-transcript-api>=0.6.0

# --- Phase 3: AI Agents ---
# LangGraph: Graph-based agent workflows (think → act → observe loop)
langgraph>=0.2.0
langchain-core>=0.3.0             # Core abstractions for LangChain/LangGraph
langchain-openai>=0.2.0           # OpenAI-compatible LLM wrapper for LangGraph

# DuckDuckGo: Free web search (no API key needed)
duckduckgo-search>=6.0.0

# --- Utilities ---
python-dotenv>=1.0.0      # Load .env files
pydantic>=2.0.0            # Data validation & settings management
pydantic-settings>=2.0.0   # Settings management with .env support
httpx>=0.25.0              # Modern async HTTP client
